/**
 * Discovery directory icons.
 *
 * Maps directory marks to image URLs or paths. When a mark is not present,
 * the row falls back to displaying the mark text itself.
 */
export const DISCOVERY_ICONS: Record<string, string> = {
  // Icons can be added here by mark as needed
  // Example: "sy": "/assets/discovery/smithery.png",
};
